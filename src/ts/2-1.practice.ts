//숫자만 담을 수 있는 배열

let onlyNumArr: number[] = [1, 2, 3];

// 문자만 담을 수 있는 배열

let onlyStrArr: string[] = ['1', '2', '3'];

// 다양한 타입을 포함할 수 있는 배열(튜플 아님)

let uni: (string | number)[] = ['hi', 1, 2, 'hi'];

// 숫자만 담을 수 있는 배열 (튜플로 정의)

let matrix: [number, number, number][] = [
  // let matrix: number[][] = [
  [1, 2, 3],
  [4, 5, 6],
  [7, 8, 9],
];
